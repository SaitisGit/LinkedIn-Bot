// util.js
const sanitize = (text) => {
    // Your implementation to sanitize text
    // Replace the following line with your actual implementation
    return text.replace(/[^a-zA-Z0-9]/g, '');
};

module.exports = sanitize;
